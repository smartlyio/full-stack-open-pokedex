name: Hello World!

on:
  push:
    branches:
      - master
      # note that your "main" branch might be called main instead of master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
  now_it_is_job:
    runs-on: ubuntu-20.4
    steps:
      - name: Now it is
      run: |
        date
  show_current_directory_job:
    runs-on:ubuntu-20.04
    steps:
    - name: show curr directory
    run: |
      ls
