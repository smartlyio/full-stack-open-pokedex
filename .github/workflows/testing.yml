name: Testing stuff

on:
  push:
    branches:
      - master

jobs:
  a_test_job:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: github context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: commits
        env:
          COMMITS: ${{ toJson(github.event.commits) }}
        run: echo "$COMMITS"
      - name: commit messages
        env:
          COMMIT_MESSAGES: ${{ toJson(github.event.commits.*.message) }}
        run: echo "$COMMIT_MESSAGES"
      # - name: Only runs if message doesn't contain "#skip" -
      #   env:
      #     COMMIT_MESSAGES: ${{ toJson(github.event.commits.*.message) }}
      #     # SKIP_CHECK: ${{ contains(env.COMMIT_MESSAGES, '#skip')}}
      #   #asd if: ${{ !env.SKIP_CHECK }}
      #   # run: echo the check is ${{ env.SKIP_CHECK }} the message is ${{ env.COMMIT_MESSAGES }}
      #   run: echo the message is ${{ env.COMMIT_MESSAGES }}
