name: Testing stuff

on:
  push:
    branches:
      - feature-2

jobs:
  a_test_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Failing step
        id: demo
        run: exit 1
      - name: commit
        if: ${{ failure()}}
        run: echo ${{ github.event.repository.homepage }}
      - name: github
        if: ${{ failure()}}
        run: |
          echo '${{ github.event.head_commit.url}} by ${{ github.event.head_commit.committer.username}} broke the build :('

  job_2:
    needs: [a_test_job]
    runs-on: ubuntu-20.04
    steps:
      - name: check
        run: echo "sda"
      - name: check 2
        if: ${{success()}}
        run: echo "success"
