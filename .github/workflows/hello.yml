name: Say hello

on:
  push:
    branches:
      - none

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
          date
      - name: Now it is
        run: |
          date
      - name: Directory content
        run: |
          ls -l
