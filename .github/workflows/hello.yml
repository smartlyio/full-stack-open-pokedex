name: Hello World

on:
  push:
    branches:
      - master

jobs:
  hello:
    runs-on: ubuntu-20.04

    steps:
    - name: Say hello
      run: |
        echo "Hello World!"

    - name: Print date
      run: |
        date

    - name: Directory content
      run: |
        ls -l