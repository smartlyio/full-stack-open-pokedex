name: test_flow

on:
  push:
    branches:
      - master

jobs:
  jepjob:
    runs-on: ubuntu-latest
    steps:
      - name: be astonished
        run: |
          echo language env is $LANG
      - name: check current directory files
        run: |
          ls -la
      - name: print date
        run: |
          date
