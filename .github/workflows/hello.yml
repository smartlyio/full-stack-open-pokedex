name: Hello World!

on: 
  push:
    branches:
      - master

jobs: 
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
     - name: Say hello
       run: |
        echo "Hello World!"
     - name: Now it is
       run: printf "%(%d-%m-%Y)T\n" $EPOCHSECONDS
     - name: Directory content
       run: ls -l  
  
