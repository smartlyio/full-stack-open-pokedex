name: 'hello world'

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    steps:
      - name: Say hello
        run: |
          echo "hello world!"
      - name: Print Date
        run: ls
      - name: Print Current Directory
        run: pwd
