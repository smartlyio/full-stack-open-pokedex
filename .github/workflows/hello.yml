name: Hello World!

on:
  push:
    branches:
      - master

permissions:
  actions: read|write

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"