name: Hello World!

on:
  push:
    branches: 
      -master

jobs:
  hellow_world_job:
    runs-on: ubuntu-20.04
    steps: 
      - name: Say hello
        run:  echo "Hello World"
      - name: Now it is
        run:  echo  date
      - name: Current Directory
        run: ls -l
