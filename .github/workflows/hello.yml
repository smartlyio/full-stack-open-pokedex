name: Hello World!

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  hello_world_job:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x]
       
    steps:
    - name: Say hello
      run: |
        echo "Hello World!"
    - name: Now it is
      run: date
    - name: Directory content
      run: ls -l
        
   