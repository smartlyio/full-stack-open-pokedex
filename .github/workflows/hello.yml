name: 'hello world'

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    steps:
      - name: Say hello
        run: |
          echo "hello world!"
      - name: Print Date
        run: date
      - name: Print Current Directory
        run: pwd
