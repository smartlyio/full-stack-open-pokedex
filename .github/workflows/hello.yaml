name: hello world!
on:
    push:
        branches:
            - master
jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo "Hello World!"
            - name: now it is
              run: date
            - name: directory content
              run: ls -l
              