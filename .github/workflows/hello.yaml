name: Hello World!

on:
  push:
    branches:
      - master
      - main

    
jobs:
  hello_world_job_ubuntu:
    runs-on: ubuntu-20.04
    steps:
      - name: say hello
        run: |
          echo "Hello World!"
          echo "Look at the date"

      - name: display date
        run: date
      
      - name: display directory
        run: ls 
      
      
  