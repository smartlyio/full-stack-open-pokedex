name: Hello World!

on:
  push:
    branches:
      - master
      - main

    
jobs:
  hello_world_job_ubuntu:
    runs-on: ubuntu-20.04
    steps:
      - name: say hello
        run: date && ls
      
      
  