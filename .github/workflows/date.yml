name: Now it is

on:
  push:
    branches:
      - master
      # note that your "main" branch might be called main instead of master

jobs:
  date:
    runs-on: windows-latest
    steps:
      - name: print date
        run: |
          date
