name: test_my_app

on:
  push:
  pull_request:
    branches:
      - master

jobs:
  run_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v4
        with:
          node-version: "21"
      - name: install dependencies
        run: npm install
      - name: run test
        run: npm run test
      - name: e2e tests
        uses: cypress-io/github-action@v6
        with:
          command: npm run test:e2e
          start: npm run start-prod &&  && echo ${cult http://localhost:5000}
          wait-on: http://localhost:5000
