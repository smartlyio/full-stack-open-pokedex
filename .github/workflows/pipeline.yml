name: Deployment Pipeline

on:
    push:
        branches:
            - master

jobs:
    simple_deployment_pipeline:
        runs-on: ubuntu-20.04
        steps:
            - uses: actions/checkout@v3
            - uses: actions/setup-node@v3
              with:
                node-version: '16'
            - name: Install dependencies
              run: npm install
            - name: Check style
              run: npm run eslint
            - name: Render GitHub Action
              uses: Bounceapp/render-action@0.6.0
              with:
                render-token: rnd_ejMELZ5QIwfnn27jBr6TBn3eNL57
                github-token: rnd_ejMELZ5QIwfnn27jBr6TBn3eNL57
                service-id: srv-ci5ft0tph6eh6mr8bnc0?key=DEv8qapxkAE
                retries: 20
                wait: 16000
                sleep: 30000
