name: Deployment pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches: [master]
    types: [opened, synchronize]
env:
  FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
  COMMIT_MESSAGES: ${{ join(github.event.commits.*.message, ', ') }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  tests:
    runs-on: ubuntu-20.04
    steps:
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Install dependencies
        run: npm install

      - name: Check style
        run: npm run eslint

      - name: eslint Failure Discord Notification
        if: ${{failure()}}
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: error
          details: Pokedek eslint failure
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
          text: ${{ github.event.commits[0].author.name }} broke the style

      - name: build
        run: npm run build

      - name: build Failure Discord Notification
        if: ${{failure()}}
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: error
          details: Pokedek build failure
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

      - name: Test
        run: npm run test

      - name: Test Failure Discord Notification
        if: ${{failure()}}
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: error
          details: Pokedek test failure
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

      - name: e2e tests
        uses: cypress-io/github-action@v5
        with:
          command: npm run test:e2e
          start: npm run start-prod
          wait-on: http://localhost:5000

      - name: e2e Test Failure Discord Notification
        if: ${{failure()}}
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: error
          details: Pokedek e2e test failure
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

  deploy:
    runs-on: ubuntu-20.04
    needs: [tests]
    if: ${{ github.event_name == 'push' && !contains(join(github.event.commits.*.message, ', '), '#skip') }}
    steps:
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: "16"
      - name: deployment
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: flyctl deploy --remote-only

      - name: Deployment Success Discord Notification
        if: ${{success()}}
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: info
          details: A new build deployed to fly.io by ${{ github.event.commits[0].author.name }}!
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

      - name: Deployment Failure Discord Notification
        if: ${{failure()}}
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: error
          details: Deployment to fly.io failed
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

  tag_release:
    needs: [deploy]
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3

      - name: Bump version and push tag
        env:
          DEFAULT_BUMP: patch
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
        if: |
          !contains(env.COMMIT_MESSAGES, '#skip')
            && github.event_name == 'push'
        # run: echo shouldDeployTagRelease
        uses: anothrNick/github-tag-action@1.55.0
