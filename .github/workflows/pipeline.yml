name: Deployment pipeline

on:
  push:
    branches:
      - master

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actoins/setup-node@v2
        with:
          node_version: '16'
      - name: npm install
        run: npm install
      - name: lint
        run: npm run eslint
