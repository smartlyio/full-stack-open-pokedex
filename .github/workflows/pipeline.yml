name: Hello World!

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo "Hello World!"
      - name: print date
        run: date
      - name: print folder
        run: ls

